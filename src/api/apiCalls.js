export const getQuestions = async () => {

};

export const reviewGrammar = async () => {
  // the headers will need to contain two api keys, in this case they will both be the PERFECT_TENSE_TEY environment variable
  
};

